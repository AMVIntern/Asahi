<UserControl x:Class="Asahi.UserControls.RecipeParameterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Asahi.UserControls"
             xmlns:sc="clr-namespace:Asahi.UserControls.SubControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{DynamicResource PrimaryBackground}"
  HorizontalAlignment="Stretch"
  VerticalAlignment="Stretch">

        <!-- Virtualized ListView for Parameters -->
        <ListView ItemsSource="{Binding RecipeParameters}"
          VirtualizingPanel.IsVirtualizing="True"
          VirtualizingPanel.VirtualizationMode="Recycling"
          ScrollViewer.CanContentScroll="True"
          BorderThickness="0"
          Padding="5 5 20 5"
          Background="{DynamicResource PrimaryBackground}"
          HorizontalContentAlignment="Stretch">

            <!-- Use VirtualizingStackPanel for performance -->
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>

            <!-- Ensure full row stretch -->
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                </Style>
            </ListView.ItemContainerStyle>

            <!-- Recipe Parameter Row Template -->
            <ListView.ItemTemplate>
                <DataTemplate>
                    <sc:RecipeParameterRow ParameterName="{Binding ParameterName}"
                                   CurrentValue="{Binding DisplayValue}"
                                   NewValue="{Binding NewValue, Mode=TwoWay}"/>
                </DataTemplate>
            </ListView.ItemTemplate>

            <!-- Custom ScrollBar Styling -->
            <ListView.Resources>
                <Style TargetType="ScrollBar" BasedOn="{StaticResource CustomScrollBar}" />
            </ListView.Resources>
        </ListView>
    </Grid>
</UserControl>
